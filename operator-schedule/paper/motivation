Motivation
1.我们使用一个真实世界的程序来解释我们的动机，The code snippet in Listing 1 is part of the cjpeg program, 
1.select_file_type(input) == BMP
2.start_input_bmp 成功完成（header 自洽、偏移/尺寸/压缩可接受）
3.biBitCount == 8 ⇒ get_pixel_rows == get_8bit_row
palette_entries = N 且 colormap 按 N 分配成功
在某个 scanline 的某个像素位置，像素索引字节 t >= N
程序执行到 colormap[*][t]
get_8bit_row() 在展开 BMP 调色板索引时，未检查像素索引 t 是否小于调色板实际大小，可在 8-bit BMP 中构造“越界的调色板索引”，导致对 colormap[*][t] 的堆越界读取，引发崩溃。
 /* Process data */
while(cinfo.next_scanline<cinfo.image_height){
 num_scanlines =(*src_mgr->get_pixel_rows)(&cinfo, src_mgr);
(void)jpeg_write_scanlines(&cinfo,src_mgr->buffer, num_scanlines);
}


一、现有方法的不足
被动方式
优化距离度量：一些工作如xx优先对更接近目标的种子进行变异，但是挑选出来的种子会被后续随机变异的策略破坏，导致偏离目标点。
比如条件1和2，即使通过了严格的条件，后续随机变异可能会破坏这些条件导致变异效率不高。
并且无法直接产生满足条件1和2的输入，即使已经偶尔有了这样的例子，也无法借用它们的经验对后续变异产生指导。

修剪无关执行：选择性插桩、调用栈引导、程序修剪等方式，在产生的巨大的输入空间中进行事后的修剪，在这个例子上仍然无法直接产生满足条件1和2的输入，即使已经偶尔有了这样的例子，也无法借用它们的经验对后续变异产生指导。	

主动方式
约束求解
另一种可能的解决方案是使用约束求解技术，例如correlation analysis（redqueen、angora）或混合执行(KLEE、QYSM）来绕过条件1和2。correlation analysis技术在应用于识别标记字节时面临挑战，因为标记字节的偏移量在不同输入中可能有所不同：由于第 8 行的循环在标记字节之前检查填充字节，污点跟踪[12, 29]可能会出现过度污点的问题，而输入到状态的关系[5, 17]可能会由于在“着色”过程中错误修改填充字节而导致不准确的识别。
对于混合执行，其固有的可扩展性问题限制了其在大型程序中重现错误的有效性，特别是在这个示例中，涉及具有嵌套while 循环的程序结构。【参考halo的说法也行】


优化输入生成
Halo利用已经生成的数据中的动态不变量对后续生成输入中的不变量范围进行约束，从而减少了输入的生成范围；WAFLGo利用……；IDFuzz则采取了一种更加智能的方式，使用神经网络的梯度信息从过往的历史变异中学习与到达目标点最有关的约束的相关字节，从而避免了使用污点或符号执行等重量级分析工具，有效的帮助生成的输入改进，从而到达目标点，这些方法专注于学习通往目标点上的路径条件（字节层面），有助于从现有的历史信息中学习如何通过路径条件比如条件1或者条件2，但是这些方法无法学习高层次的语义知识，以及在通过低层次的字节层面条件时无法做到更高效，我们后续将进一步说明。



二、从历史信息中学习高级语义知识
当主动方式被认为是更有潜力的方法[]，目前的从过往历史信息中学习指导未来变异方式的方法，主要还是集中在学习如何指导克服约束(字节层面信息）到达目标点，从而遗留了一大部分丰富的语义知识未加利用，我们的观察是在过往变异信息中，存在更高级的语义知识可以被我们学习到从而指导未来的输入生成，下面我们将具体说明是什么样的语义知识。
1.全局状态语义
在fuzzing 过程中本身存在全局的语义状态，比如距离上次新路径的时间、最近进展的稀疏程度、是否到达目标点，它们不描述输入“是什么”，而是描述 fuzzing 当前在“做什么 / 卡在哪里 / 需要什么”。全局状态语义描述了fuzzer和program之间的当前交互状态，反映了当前需要的进展类型，而不是输入的样子。
动机例子上怎么展示全局状态，关于这里的说法还比较模糊 
我觉得需要展示一些时间下，例子的产生情况是怎么样的
我们认为这种信息蕴含了指导未来输入生成的知识，比如在我们动机例子上，没有到达目标点前，卡在条件1这个具体约束上，更需要变动少量字节为BMP等；已经到达了目标点，更需要while循环中来构造数据来指导数据部分的输入生成，这些知识有助于我们指导未来的输入生成。（这里讲的可能太细致了，方案里的summary做不到这么细致的解释就不要这样提，只提一个大概的）
（从历史到现在的状态，持续性的，是一个summary）
怎么能从summary中得到具体的指导信息    reward的状态
AFL的探索和利用阶段和这个的区别，要说明前者在我们的场景下需要更细化的指导信息

fuzz中可能有类似这种工作 state aware


2.局部结构语义
1.同一输入的可复用性
一些输入中，本身存在有助于后续输入生成的字节或者字节片段，在我们的动机例子上，需要biBitCount == 8，与输入中一个字节相关，需要其为08，而这个字节有很大概率随机出现在其他位置上，比起使用约束求解解出来这个值，我们可以想办法利用到已经存在的这个值；以及例子上，while代码段是逐行读取像素行，后续行会重复同一个“行读取模板”，如case1的xxx字段，作为第一行被成功读取，后面每一行只需要“结构一致、长度正确”，libjpeg 就会一行一行地继续读取下去，意味着例子本身的一个片段可以被重复利用，来构造合法的输入。很多程序的种子在读取的时候均可以这样，如xml，json等均存在结构上的重复。

2. 不同输入的可复用性
在我们的动机例子上，libjpeg中负责“格式识别、颜色空间、像素行读取、JPEG 编码”的代码片段是解耦的：
[文件格式识别]-》[源管理器 src_mgr]-》[start_input]-》[get_pixel_rows] -》[jpeg_write_scanlines]
虽然 24bit 与 8bit 像素数据在解析语义上不兼容，但由于 cjpeg 在不同格式之间共享 scanline 消费框架，一旦 format-gating 字段（如 bit depth）被修改，原本属于其他格式的 8bit payload 就可能与新的解析路径语义对齐，从而变得可用。
一个 case 原本走的是 24bit 解码路径，而另一个 case 来自 8bit 格式（如 TGA）；当通过变异使前者的 bit-depth 决策发生变化后，后者中已经组织良好的 8bit 像素数据就可能被复用，并对触发目标路径产生帮助。
ratarga.c里面有同样的get_8bit_row函数，因此case 1 走了BMP+24字段+while那里调用get-24bitrow，后续也按照24bit的数据格式组织，case2 走了TAG+8字段+同样走了一个while那里调用get-8bitrow，后者对于前者组织读取8bit的像素行有帮助。
这里的语义知识2最终确定为：case 1:  bmp头+ Bitbitcount==8 ， 错误的不符合8bit的数据组织格式像素行；case 2：TAG头+ Bitbitcount==8 + 正确的数据组织格式。所以case2的数据组织格式可以被case1使用，不同格式 / 不同 bit-depth 的 case 天然可以在结构上互补。

因此，我们的观察是在过往变异信息中，存在全局和局部的语义知识可以被我们学习到从而指导未来的输入生成，虽然直接学习这种状态和结构上的语义关系很难，
（讲清楚算子是语义体现的宏观上的一种说法，先说全局是算子的xxx特性，再说局部是算子的xxx特性，下面的太具体了）
但是我们可以聚焦到变异算子的使用上来解决这些问题：
1.全局状态中，当观测到stagnation + reward 全为 0，使用结构性算子（splice / clone / insert）有助于突破瓶颈，打开新结构，当观测到reward 来自微小的增长，使用精细的算子更合适。（这里讲的可能太细致了，方案里的summary做不到这么细致的解释就不要这样提，只提一个大概的）
2.使用clone算子复制同一输入的本身，可以达到利用同一输入本身的目的，如果我们观测到这种好的使用，学习到并且后续加以使用，也可以利用clone来复制同一个输入中的片段，来达到更快的构造数据的目的，比如我们动机例子中的while结构。
3.使用splice算子可以利用不同输入中的可利用部分，比如在我们的动机例子中，拼接case1的x部分和case2的x部分，有助于我们得到更好的输入。

尽管无法精确定位路径约束相关字节，但是我们通过这种方式可以学习到过去无法学习到的状态和结构信息，以及过往IDFuzz这种专注解决的路径约束的学习方式，我们可能可以通过整体结构的拼接或者重复利用更快的达到多个路径条件，比如动机例子上，我们可能可以通过一次性复制合理的BMP以及header来更快的满足路径条件，后续的实验以及casestudy表明这种大范围的语义知识可以与idfuzz这种精细的字节知识形成互补，在大部分情况下甚至更有效。
   
	
挑战（方案设计上的挑战）
尽管使用变异算子的学习和使用来学习DGF变异历史信息中更为高级的语义知识看起来很有潜力，但是想实际学习一套合理的算子调度方案仍然是困难的，过往有一些fuzz中学习变异算子调度的方案，比如MOpt和DARWIN，分别使用粒子群优化算法和进化算法来学习算子的调度，但是这些都是针对以coverage为主的fuzz，在我们的dgf场景中会变得失效：理由1（反馈稀疏和不平稳），和不够有效（不足）：理由2（存在负反馈）（如果我们的方案解决不了就不要这么细致，稍微提两句就好了）；以及在我们的场景下，这些不考虑算子位置的全局的过往场景下足够的算子调度方案不足会放大，动机例子中……(这里还是需要强有力的讲一讲和过往调度算子的工作有什么区别）

所以我们需要一套dgf场景下，联合位置和算子学习使用的方案，想要这样一套方案我们面对两个主要挑战：
C1:
这种情况需要考虑的变量和状态信息太过于复杂，就是如果学习不当，学习不到有意义的知识，或者学到的知识过于粗糙，起不到宏观上调控的作用，基本和随机差不多

解决方案：两阶段分层次学习不同的东西，先算子使用后位置使用


C2:
一个case上的学习知识怎么迁移
SMR




问题：
1.全局的落实到算子的使用上现在生硬吗，主要是全局的一些状态我还没想好，定义好
2.怎么定义全局的状态，现在的太散，我想只集中到一两个上，然后要和设计的方法结合上，还是这个得从实际效果出发
3.方案的设计需要联合动机，但是动机我又没办法更加细化



问题1：如何证明 operator ≠ 只是 scheduling
你要证明：
在结构型目标中
某些 operator 变得 dominant
且这种 dominance 与结构复用有关
如果你能 show：
splice usage ↑ when cross-input complementarity exists
clone usage ↑ when repetition needed
byte flip usage ↑ near constraint fine-tuning
那你就真的在学语义。
✅ 我们观察到结构语义无法被字节级方法表达
✅ 而 mutation operator 是结构变换的基本单位
✅ 因此我们学习 operator usage policy
✅ 从而间接学习结构语义
不要强调“我们发明了算子调度”。
而是强调：
Operator selection is the vehicle through which structural semantics are expressed and learned.
把算子作为“表达层”，而不是“贡献点”。