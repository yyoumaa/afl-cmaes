xx：从历史信息中学习结构和语义知识用来指导变异for DGF
摘要

Introduction
1.模糊测试已成为检测软件系统漏洞最有效的技术之一。根据测试目的的不同，它可以大致分为两种类型：基于覆盖的灰盒模糊测试（CGF）和定向灰盒模糊测试（DGF）。与 CGF（[5， 8]）侧重于探索新的执行路径并最大化代码覆盖范围不同，DGF（[7， 10]）则采用更具针对性的策略，引导模糊测试过程朝着疑似存在漏洞的特定程序位置进行。DGF 已应用于各种场景[35]，例如补丁测试、崩溃重现、静态分析报告的验证以及概念验证的生成，从而使开发人员能够更高效地验证、分析和修复安全问题。
2.许多DGF的解决方法已经被提出，它们大致分为两部分，设计精细的距离指标选取那些更“靠近”目标点的种子，比如程序控制流距离引导[...]、数据流距离引导、偏差基本块距离引导等；和提前终止无法到达目标点的执行，包括修建无关程序、使用选择性插桩，和使用调用栈提前终止程序状态等。然而这些方法始终是一些“事后”的补偿措施，前者精心挑选的种子会被目前随机变异的机制破坏掉，使得一些PoC甚至是从较远的种子中复现的[...]，后者减少的无关执行比例十分有限[Halo]，这两种方法根本的局限性在于没有主动的从输入生成的方式进行改进，仍然保留AFL风格的随机变异风格，遗留了非常大的改进空间。
3.最近有一些工作开始主动的从输入生成的方式改进，Halo利用已经生成的数据中的动态不变量对后续生成输入中的不变量范围进行约束，从而减少了输入的生成范围；WAFLGo利用……；IDFuzz则采取了一种更加智能的方式，使用神经网络的梯度信息从过往的历史变异中学习与到达目标点最有关的约束的相关字节，从而避免了使用污点或符号执行等重量级分析工具，有效的帮助生成的输入改进，从而到达目标点。
4.然而，即使过往历史信息中有关约束相关字节的定位[idfuzz]和取值[halo]被学习利用到，我们认为仍然遗留可以指导未来变异的丰富的高级信息，尤其是一些结构和语义信息没有被学习和利用到，包括……和……，……。
5.我们的核心观察是
6.这里也许可以在完善，参考idfuzz的intro
7.因此我们提倡学习和利用算子的选择和使用，来表征这种高级的语义信息，从而进一步提高输入生成的质量。具体来说，我们……
8.……继续介绍方法
9.……继续介绍方法
10.总体来说，我们的贡献有：
- 我们发现了现有的改进输入生成的dgf方法只学习字节层面的不足，提出使用历史变异信息中更高级的语义和结构信息来指导未来输入生成，并且落实到算子的选择和使用上
- 提出了在DGF上的一套算子的选择和使用区域的方法，利用……，包括，这些方法有助于构建一套
- 实现了我们系统的原型，在标准测试集上通过和各种最先进的方法对比，结果显示我们方法的优越性